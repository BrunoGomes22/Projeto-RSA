package org.nap.fleetman.server.mission.dsl

class DegreeCategory {
    static Number getDegree(Number num) {
        return num
    }

    static Number getDegrees(Number num) {
        return num
    }

    static Number getDeg(Number num) {
        return num
    }
}